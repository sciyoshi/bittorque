# This is the default configuration file for BitTorque.

# Adjusts various protocol options.
[Protocol]

# Whether to use Message Stream Encryption for outgoing connections.
# 1: Never use encryption.
# 2: Try encryption first, but fallback to unencrypted if peer doesn't accept.
# 3: Only use encrypted connections.
OutgoingEncryption = 1
